global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'quote-api-monitor'

scrape_configs:
  - job_name: 'quote-api'
    static_configs:
      - targets: ['localhost:3000']
    metrics_path: '/metrics'
    scrape_interval: 10s
    scrape_timeout: 5s
